{
  "name": "atcourse",
  "version": "0.1.0",
  "author": "",
  "license": "MIT",
  "private": true,
  "main": "index.js",
  "scripts": {
    "build": "pnpm --recursive --parallel --stream build",
    "dev": "NODE_ENV=development pnpm --recursive --parallel --stream dev",
    "prod": "NODE_ENV=production pnpm --recursive --parallel --stream prod"
  },
  "packageManager": "pnpm@8.15.9",
  "workspaces": {
    "packages": [
      "client",
      "server",
      "common/*"
    ]
  }
}